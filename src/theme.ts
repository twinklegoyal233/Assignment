import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3CD6BE',
      light: '#F0FFFD',
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
});

export default theme;